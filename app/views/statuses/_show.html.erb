<!-- Status show -->

<%= status.author.name %>
<%= status.text %>
<%= render 'likes/like', :post => status %>

<% if current_user == status.author %>
<%= button_to "Edit Status", edit_status_url(status), method: :get %>
<%= button_to "Remove Status", status_url(status), method: :delete %>
<% end %>