<!-- Index of Pending Friendships -->

<% @reqs_received.each do |req| %>
<%= render "received_request",
:user => PendingFriendship.find(req.user_id) %>
<% end %>

<% @reqs_sent.each do |req| %>
<%= render "sent_request",
:user => PendingFriendship.find(req.pending_friend_id)%>
<% end %>

