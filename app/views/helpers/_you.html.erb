<img src="<%= user.profile.photo_url %>"></img>

<section>
  <%= user.name %>
  <%= user.email %>
</section>

<section>
  <button >Timeline</button>
  <%= button_to "About", profile_url(current_user.profile), method: :get %>
  <button >Photos</button>
  <%= button_to "Friends", user_friendships_url(current_user), method: :get %>
</section>